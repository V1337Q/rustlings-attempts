fn main() {
    // TODO: Change the line below to fix the compiler error.
    let x: i32 = 10;
    println!("Number {x}");
    // Missing value on variable
    
    //: i32 is for declaring a file type explicitly.
    
    // The other expression would be:
    let y: i32;
    y = 5;
    println!("y: {}", y);
        

}
